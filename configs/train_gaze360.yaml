# Gaze360 Training Configuration

data:
  root: datasets/Gaze360
  dataset: gaze360

model:
  name: wdmpa_net
  pretrained: null

train:
  epochs: 60
  batch_size: 32
  lr: 0.0001
  weight_decay: 0

output:
  dir: runs/train
  name: gaze360

device: "0"
workers: 8
